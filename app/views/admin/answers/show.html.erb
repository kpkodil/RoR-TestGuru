<h1><%= @answer.question.title %></h1>

<h2><%= @answer.title %></h2>

<p>
  <%= t('.correct')%>:<%= @answer.correct %>
</p>

<%= link_to t('.edit'), edit_admin_answer_path(@answer) %> |
<%= link_to t('.back_to_question', question: @answer.question.title), admin_question_path(@answer.question) %> |
<%= link_to t('.delete', answer: @answer.title), admin_answer_path(@answer), method: :delete %><br>
