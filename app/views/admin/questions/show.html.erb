<h1><%= @question.title %></h1>

<h2>Answers:</h2>

<ol>
    <%= render @question.answers %>
</ol>

<%= link_to 'New Answer', new_admin_question_answer_path(@question) %><br>
<%= link_to "Back to #{ @question.test.title } questions list", admin_test_path(@question.test_id) %><br>
<%= link_to "Delete #{@question.title} question", admin_question_path(@question), method: :delete %><br>