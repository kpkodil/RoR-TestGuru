<% question = Question.find(params[:id]) %>
<h1><%= question.title %></h1>

<%= link_to "Back to questions List", test_questions_path(test_id: question.test_id) %>
<%= link_to "Delete", question_path(params[:id]) , method: :delete %>